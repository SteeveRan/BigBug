/**
 * @file GitMirroring/index.tsx
 * @description Git Mirroring v2 — placeholder page for future implementation
 */

import { Typography, Card } from 'antd';

const { Title, Paragraph } = Typography;

const GitMirroring = () => {
  return (
    <Card>
      <Title level={3}>Git Mirroring</Title>
      <Paragraph>
        Git repository mirroring configuration will be available here.
      </Paragraph>
    </Card>
  );
};

export default GitMirroring;
