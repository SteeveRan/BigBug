import { Typography, Card, Empty } from 'antd';

const DashboardPage = () => (
  <Card>
    <Typography.Title level={4}>Git Mirroring Dashboard</Typography.Title>
    <Empty description="Dashboard will be available in a future stage." />
  </Card>
);

export default DashboardPage;
